---
- hosts: all
  gather_facts: False
  tasks:
    - name: 'Install Ansible'
      apt:
        name: aptitude
        update_cache: yes

    - name: 'Upgrade all packages'
      apt:
        update_cache: yes
        upgrade: full
